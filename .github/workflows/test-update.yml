name: Install MSYS2

on: [push, pull_request]

jobs:
  build:
    name: >-
      Install MSYS2
    runs-on: 'windows-latest'
    strategy:
      fail-fast: false
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Update MSYS2
        run:  ./update_msys2.ps1
